(() => { const background = '<div class="interactive-bg" aria-hidden="true"><div class="wallpaper"></div><div class="grid-bg" id="gridBg"></div><div class="mouse-glow" id="mouseGlow"></div><i class="floating-dot dot-1"></i><i class="floating-dot dot-2"></i><i class="floating-dot dot-3"></i></div>'; if (!document.querySelector('.interactive-bg')) document.body.insertAdjacentHTML('afterbegin', background); document.title = document.title.replaceAll('王家琪', '近心'); const description = document.querySelector('meta[name="description"]'); if (description) description.content = description.content.replaceAll('王家琪', '近心'); document.querySelectorAll('.brand').forEach(brand => { brand.innerHTML = 'JINXIN<span>·</span>NOTES'; }); const walker = document.createTreeWalker(document.body, NodeFilter.SHOW_TEXT); let node; while (node = walker.nextNode()) node.nodeValue = node.nodeValue.replaceAll('王家琪', '近心').replaceAll('Wang Jiaqi', '近心'); const stored = localStorage.getItem('wjq-theme'); if (stored === 'light') document.body.classList.add('light'); const button = document.querySelector('.theme-toggle'); if (button) button.addEventListener('click', () => { document.body.classList.toggle('light'); localStorage.setItem('wjq-theme', document.body.classList.contains('light') ? 'light' : 'dark'); }); const glow = document.getElementById('mouseGlow'); const grid = document.getElementById('gridBg'); if (!glow || !grid || matchMedia('(prefers-reduced-motion: reduce)').matches) return; let tx = innerWidth / 2, ty = innerHeight / 2, x = tx, y = ty; addEventListener('mousemove', event => { tx = event.clientX; ty = event.clientY; const dx = tx / innerWidth - .5, dy = ty / innerHeight - .5; grid.style.transform = `translate(${dx * 9}px,${dy * 9}px)`; }, { passive: true }); const tick = () => { x += (tx - x) * .075; y += (ty - y) * .075; glow.style.transform = `translate(${x}px,${y}px) translate(-50%,-50%)`; requestAnimationFrame(tick); }; tick(); })();
