# 写新文章

1. 复制 `post-template.md`，重命名为英文短横线文件名，例如 `my-new-post.md`。
2. 填写顶部信息和正文。
3. 在项目根目录运行 `npm run build`。
4. 检查生成的 `posts/my-new-post.html` 与 `blog.html`，再提交推送。

Markdown 支持二级标题、无序列表、链接、图片、C++/Python/Bash 等代码块，以及 `$$ ... $$` 块级公式和 `$ ... $` 行内公式。
